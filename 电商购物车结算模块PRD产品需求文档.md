# 电商购物车结算模块PRD（产品需求文档）

## 一、文档基础信息

| 项目名称 | 电商购物车结算模块（设计模式课程项目） |
|---------|-------------------------------------|
| PRD版本 | V2.0 |
| 作者 | 设计模式课程项目组 |
| 撰写日期 | 2026年4月20日 |
| 适用范围 | 本科设计模式课程项目，用于演示设计模式在电商促销场景中的应用 |
| 修订历史 | V1.0：初始版本；V2.0：全面重构，完善功能需求和设计模式说明 |

## 二、产品概述

### 2.1 项目背景

本项目为本科设计模式课程实践项目，以电商购物车结算模块为核心场景，针对电商促销活动"规则多变、需求迭代快"的特点，通过应用11种经典设计模式，实现促销规则的灵活适配、低耦合扩展，解决传统代码中促销逻辑混乱、新增/修改规则成本高的问题，直观体现设计模式在实际业务中的应用价值。

### 2.2 产品定位

- **教学定位**：设计模式课程的实践案例，展示设计模式的实际应用
- **功能定位**：完整的电商购物车结算系统，包含购物车管理、促销计算、订单处理等核心功能
- **技术定位**：Java Swing桌面应用，采用分层架构，高内聚低耦合

### 2.3 核心目标

| 目标类型 | 具体目标 | 衡量指标 |
|---------|---------|---------|
| 功能目标 | 实现完整的购物车结算流程 | 覆盖购物车管理、结算、订单、会员、促销、物流、评价等模块 |
| 设计目标 | 合理应用11种设计模式 | 每种模式都有明确的业务场景和应用价值 |
| 质量目标 | 代码可维护、可扩展 | 新增功能无需大量修改原有代码 |
| 教学目标 | 清晰展示设计模式应用 | 代码注释完整，文档清晰 |

### 2.4 用户画像

| 用户类型 | 描述 | 核心需求 |
|---------|------|---------|
| 购物者 | 普通消费者 | 浏览商品、管理购物车、结算下单 |
| 会员用户 | 注册会员 | 享受会员折扣、积分累积 |
| 商家 | 商品销售方 | 管理商品、查看销售数据 |
| 管理员 | 系统管理者 | 用户管理、促销管理、系统监控 |
| 课程教师 | 项目评审者 | 考核设计模式应用合理性 |
| 开发者 | 项目实现者 | 代码实现与答辩讲解 |

### 2.5 功能范围

#### 包含功能
1. 购物车基础操作（添加、修改、删除、查询商品）
2. 多类型促销活动（优惠券、满减、VIP折扣、限时折扣、满赠）的匹配与计算
3. 促销规则管理（新增、修改、删除、查询）
4. 结算金额实时计算与可视化展示
5. 订单生成、状态管理与历史查询
6. 会员等级识别、积分管理与折扣适配
7. 收货地址管理
8. 物流跟踪与发货确认
9. 商品评价与评分统计
10. 日志记录与查看
11. 操作撤销功能
12. 管理员后台（会员管理、订单管理、促销管理、商品管理、销售统计、评价管理）

#### 不包含功能
1. 复杂用户权限管理（仅实现简单角色切换）
2. 真实支付接口对接（仅模拟支付流程）
3. 高并发处理与分布式部署
4. 商品库存管理
5. 真实物流对接

## 三、功能需求详解

### 3.1 购物车管理模块

#### 3.1.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| C-001 | 添加商品到购物车 | P0 | 用户选择商品，输入购买数量，添加到购物车 |
| C-002 | 修改商品数量 | P0 | 用户修改购物车中商品的数量 |
| C-003 | 删除商品 | P0 | 用户从购物车中删除商品 |
| C-004 | 查看购物车 | P0 | 展示购物车中所有商品及总价 |
| C-005 | 分类筛选 | P1 | 按商品分类筛选购物车商品 |
| C-006 | 商品搜索 | P1 | 根据关键词搜索购物车商品 |
| C-007 | 查看商品详情 | P1 | 双击商品查看详情和评价 |
| C-008 | 操作撤销 | P1 | 撤销添加、删除、修改操作 |

#### 3.1.2 功能详细说明

**C-001 添加商品到购物车**

| 项目 | 说明 |
|-----|------|
| 功能描述 | 用户从商品列表中选择商品，输入购买数量，添加到购物车 |
| 输入 | 商品ID、购买数量 |
| 输出 | 添加成功提示、更新后的购物车列表 |
| 业务规则 | 1. 数量必须大于0；2. 同一商品累加数量 |
| 设计模式 | 工厂模式（创建商品对象）、命令模式（支持撤销） |
| 异常处理 | 数量非法时提示错误 |

**C-002 修改商品数量**

| 项目 | 说明 |
|-----|------|
| 功能描述 | 用户通过加减按钮修改购物车中商品的数量 |
| 输入 | 商品ID、新数量 |
| 输出 | 更新后的购物车列表和总价 |
| 业务规则 | 数量不能小于1 |
| 设计模式 | 命令模式（支持撤销）、观察者模式（实时更新UI） |
| 异常处理 | 数量非法时恢复原值 |

**C-003 删除商品**

| 项目 | 说明 |
|-----|------|
| 功能描述 | 用户从购物车中删除指定商品 |
| 输入 | 商品ID |
| 输出 | 删除成功提示、更新后的购物车列表 |
| 业务规则 | 需要先选中商品 |
| 设计模式 | 命令模式（支持撤销）、观察者模式（实时更新UI） |
| 异常处理 | 未选中商品时提示 |

**C-008 操作撤销**

| 项目 | 说明 |
|-----|------|
| 功能描述 | 撤销上一步购物车操作 |
| 输入 | 无 |
| 输出 | 恢复到操作前的状态 |
| 业务规则 | 仅支持撤销添加、删除、修改数量操作 |
| 设计模式 | 命令模式（CommandManager管理命令栈） |
| 异常处理 | 无可撤销操作时提示 |

### 3.2 促销规则管理模块

#### 3.2.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| P-001 | 新增促销规则 | P0 | 管理员新增促销规则 |
| P-002 | 修改促销规则 | P0 | 管理员修改促销规则参数 |
| P-003 | 删除促销规则 | P0 | 管理员删除促销规则 |
| P-004 | 查询促销规则 | P0 | 查看所有促销规则 |
| P-005 | 优惠券管理 | P0 | 新增、修改、删除优惠券 |

#### 3.2.2 促销类型定义

| 促销类型 | 类型标识 | 计算规则 | 示例 |
|---------|---------|---------|------|
| 优惠券 | COUPON | 固定金额抵扣 | 满100减20 |
| 满减 | FULL_REDUCTION | 满足门槛后固定减免 | 满200减50 |
| VIP折扣 | VIP_DISCOUNT | 按会员等级折扣 | VIP会员9折 |
| 限时折扣 | TIME_LIMIT_DISCOUNT | 限时折扣比例 | 限时8折 |
| 满赠 | GIFT | 满足条件赠送商品 | 满100送赠品 |

#### 3.2.3 功能详细说明

**P-001 新增促销规则**

| 项目 | 说明 |
|-----|------|
| 功能描述 | 管理员新增促销规则，设置规则类型、参数、优先级 |
| 输入 | 促销类型、规则参数（门槛金额、优惠值）、优先级 |
| 输出 | 新增成功提示、更新后的规则列表 |
| 业务规则 | 1. 同类型规则可存在多个；2. 优先级决定计算顺序 |
| 设计模式 | 工厂模式（创建策略对象）、策略模式（封装计算逻辑） |
| 异常处理 | 参数非法时提示错误 |

### 3.3 结算管理模块（核心模块）

#### 3.3.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| S-001 | 选择收货地址 | P0 | 用户选择或管理收货地址 |
| S-002 | 选择优惠券 | P0 | 用户选择使用的优惠券 |
| S-003 | 选择支付方式 | P0 | 用户选择支付方式 |
| S-004 | 计算优惠金额 | P0 | 系统自动计算各项优惠 |
| S-005 | 提交订单 | P0 | 用户确认并提交订单 |
| S-006 | 优惠明细展示 | P0 | 可视化展示优惠计算过程 |

#### 3.3.2 结算流程设计

```
步骤1: 选择收货地址 (25%)
    ↓
步骤2: 选择优惠券 (50%)
    ↓
步骤3: 选择支付方式 (75%)
    ↓
步骤4: 确认订单 (100%)
```

#### 3.3.3 功能详细说明

**S-004 计算优惠金额**

| 项目 | 说明 |
|-----|------|
| 功能描述 | 系统按职责链顺序依次计算各项优惠 |
| 输入 | 购物车总金额、会员信息、选中优惠券 |
| 输出 | 各项优惠金额、最终支付金额 |
| 业务规则 | 优惠计算顺序：优惠券 → 满减 → VIP折扣 |
| 设计模式 | 职责链模式（链式处理）、策略模式（各节点计算策略）、装饰器模式（动态装饰商品价格） |
| 可视化 | 分步骤展示计算过程，每步间隔0.5秒 |

**职责链计算流程**

```
订单信息
    ↓
┌─────────────────┐
│ CouponHandler   │ → 优惠券抵扣
└────────┬────────┘
         ↓
┌─────────────────┐
│ FullReduction   │ → 满减抵扣
│ Handler         │
└────────┬────────┘
         ↓
┌─────────────────┐
│ VipHandler      │ → VIP折扣
└────────┬────────┘
         ↓
    最终金额
```

**S-005 提交订单**

| 项目 | 说明 |
|-----|------|
| 功能描述 | 用户确认订单信息后提交，完成支付 |
| 输入 | 订单确认指令、支付方式 |
| 输出 | 订单号、支付结果 |
| 业务规则 | 1. 支付成功后更新会员积分；2. 订单状态变更为"已支付" |
| 设计模式 | 状态模式（订单状态转换）、模板方法模式（订单处理流程）、适配器模式（支付方式适配） |
| 异常处理 | 支付失败时提示重试 |

### 3.4 订单管理模块

#### 3.4.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| O-001 | 查看订单历史 | P0 | 查看历史订单列表 |
| O-002 | 查看订单详情 | P0 | 查看订单详细信息 |
| O-003 | 订单状态管理 | P0 | 管理员管理订单状态 |
| O-004 | 物流跟踪 | P1 | 查看物流信息 |
| O-005 | 确认收货 | P1 | 用户确认收货 |
| O-006 | 取消订单 | P1 | 取消未支付订单 |

#### 3.4.2 订单状态流转

```
┌──────────────┐     支付成功     ┌──────────────┐
│   待支付     │ ──────────────→ │   已支付     │
│ PendingPayment│                 │    Paid      │
└──────────────┘                 └──────┬───────┘
       │                                │
       │ 取消                           │ 发货
       ↓                                ↓
┌──────────────┐                 ┌──────────────┐
│   已取消     │                 │   已发货     │
│  Cancelled   │                 │   Shipped    │
└──────────────┘                 └──────┬───────┘
                                        │
                                        │ 确认收货
                                        ↓
                                 ┌──────────────┐
                                 │   已完成     │
                                 │  Completed   │
                                 └──────────────┘
```

#### 3.4.3 功能详细说明

**O-003 订单状态管理**

| 项目 | 说明 |
|-----|------|
| 功能描述 | 管理员可对订单进行发货、取消等操作 |
| 输入 | 订单ID、操作类型 |
| 输出 | 操作结果、更新后的订单状态 |
| 业务规则 | 1. 待支付订单可取消；2. 已支付订单可发货；3. 已发货订单可确认完成 |
| 设计模式 | 状态模式（OrderStateMachine管理状态转换） |
| 异常处理 | 非法状态转换时提示错误 |

### 3.5 会员管理模块

#### 3.5.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| M-001 | 会员信息展示 | P0 | 展示当前会员信息 |
| M-002 | 会员选择 | P0 | 选择当前购物会员 |
| M-003 | 会员等级管理 | P1 | 管理员管理会员等级 |
| M-004 | 积分管理 | P1 | 查看和调整会员积分 |

#### 3.5.2 会员等级定义

| 等级 | 名称 | 折扣率 | 积分门槛 |
|-----|------|--------|---------|
| 0 | 普通会员 | 0% | 0 |
| 1 | 铜牌会员 | 5% | 100 |
| 2 | 银牌会员 | 8% | 500 |
| 3 | 金牌会员 | 10% | 1000 |
| 4 | 钻石会员 | 15% | 5000 |

#### 3.5.3 功能详细说明

**M-001 会员信息展示**

| 项目 | 说明 |
|-----|------|
| 功能描述 | 在购物车页面展示当前会员信息 |
| 输出 | 会员名称、等级、积分、专属优惠金额 |
| 业务规则 | 未登录时显示"未登录" |
| 设计模式 | 策略模式（不同等级折扣策略） |

### 3.6 地址管理模块

#### 3.6.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| A-001 | 查看地址列表 | P0 | 查看所有收货地址 |
| A-002 | 新增地址 | P0 | 添加新的收货地址 |
| A-003 | 编辑地址 | P0 | 修改已有地址 |
| A-004 | 删除地址 | P0 | 删除收货地址 |
| A-005 | 设置默认地址 | P1 | 设置默认收货地址 |
| A-006 | 搜索地址 | P1 | 根据关键词搜索地址 |

### 3.7 物流管理模块

#### 3.7.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| L-001 | 物流信息展示 | P1 | 展示物流跟踪信息 |
| L-002 | 发货确认 | P1 | 管理员确认发货 |
| L-003 | 物流状态更新 | P1 | 更新物流状态 |

#### 3.7.2 物流状态定义

| 状态 | 描述 |
|-----|------|
| 待发货 | 订单已支付，等待发货 |
| 已发货 | 商品已发出 |
| 运输中 | 商品运输中 |
| 派送中 | 商品派送中 |
| 已签收 | 商品已签收 |

### 3.8 评价管理模块

#### 3.8.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| R-001 | 提交评价 | P0 | 用户对已购商品进行评价 |
| R-002 | 查看评价 | P0 | 查看商品评价列表 |
| R-003 | 评价统计 | P1 | 统计商品评分 |
| R-004 | 管理评价 | P1 | 管理员管理评价 |

### 3.9 管理员后台模块

#### 3.9.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| AD-001 | 会员管理 | P0 | 管理会员信息和等级 |
| AD-002 | 订单管理 | P0 | 管理订单状态 |
| AD-003 | 促销管理 | P0 | 管理促销规则和优惠券 |
| AD-004 | 商品管理 | P0 | 管理商品信息 |
| AD-005 | 销售统计 | P1 | 查看销售数据统计 |
| AD-006 | 评价管理 | P1 | 管理商品评价 |
| AD-007 | 系统日志 | P1 | 查看系统操作日志 |

### 3.10 商品浏览模块

#### 3.10.1 功能列表

| 功能编号 | 功能名称 | 优先级 | 描述 |
|---------|---------|--------|------|
| B-001 | 商品列表展示 | P0 | 展示所有商品 |
| B-002 | 商品分类浏览 | P0 | 按分类浏览商品 |
| B-003 | 商品搜索 | P1 | 根据关键词搜索商品 |
| B-004 | 添加到购物车 | P0 | 从浏览页添加商品到购物车 |

## 四、非功能性需求

### 4.1 性能需求

| 指标 | 要求 |
|-----|------|
| 响应时间 | 界面操作响应时间 < 500ms |
| 优惠计算 | 优惠计算时间 < 1s |
| 数据持久化 | 数据保存响应时间 < 1s |

### 4.2 可用性需求

| 指标 | 要求 |
|-----|------|
| 易学性 | 新用户5分钟内完成首次下单 |
| 易用性 | 核心功能操作步骤 ≤ 3步 |
| 错误提示 | 所有错误都有明确的提示信息 |
| 帮助支持 | 关键操作有提示说明 |

### 4.3 可维护性需求

| 指标 | 要求 |
|-----|------|
| 代码规范 | 遵循Java编码规范 |
| 注释覆盖 | 核心类和方法有完整注释 |
| 设计模式 | 每种模式有明确的应用说明 |
| 文档完整 | 有完整的README和设计文档 |

### 4.4 可扩展性需求

| 指标 | 要求 |
|-----|------|
| 新增促销类型 | 仅需新增策略类，无需修改核心逻辑 |
| 新增支付方式 | 仅需新增适配器类 |
| 新增商品类型 | 仅需扩展工厂类 |

## 五、设计模式应用说明

### 5.1 设计模式总览

| 模式类型 | 设计模式 | 应用场景 | 核心类 |
|---------|---------|---------|--------|
| 创建型 | 单例模式 | 日志、连接池、外观、事件总线、状态机 | Logger, DBConnection, ECommerceFacade, EventBus, OrderStateMachine |
| 创建型 | 工厂模式 | 商品创建、策略创建 | ECommerceFactory |
| 创建型 | 抽象工厂模式 | 统一创建产品和策略 | AbstractFactory, ECommerceFactory |
| 结构型 | 适配器模式 | 支付方式适配 | PaymentAdapter, AlipayAdapter, WechatPayAdapter等 |
| 结构型 | 装饰器模式 | 商品价格动态装饰 | ProductDecorator, DiscountDecorator, PromotionDecorator |
| 结构型 | 外观模式 | 系统统一入口 | ECommerceFacade |
| 行为型 | 策略模式 | 促销策略计算 | PromotionStrategy, StrategyContext |
| 行为型 | 职责链模式 | 折扣计算流程 | Handler, CouponHandler, FullReductionHandler, VipHandler |
| 行为型 | 观察者模式 | 购物车状态通知 | Observer, Subject, EventBus |
| 行为型 | 命令模式 | 操作撤销、批处理 | Command, CommandManager |
| 行为型 | 模板方法模式 | 订单处理流程 | OrderProcessTemplate, NormalOrderProcessor, VipOrderProcessor |
| 行为型 | 状态模式 | 订单状态管理 | OrderState, OrderStateMachine |

### 5.2 设计模式详细说明

#### 5.2.1 单例模式（Singleton Pattern）

**应用场景**：需要全局唯一实例的场景

**实现方式**：枚举实现（线程安全、简洁）

**应用实例**：

| 类名 | 作用 | 单例必要性 |
|-----|------|-----------|
| Logger | 日志记录器 | 确保日志输出到同一文件 |
| DBConnection | 数据库连接池 | 避免连接冗余 |
| ECommerceFacade | 系统外观 | 确保唯一入口 |
| EventBus | 事件总线 | 确保事件中心唯一 |
| OrderStateMachine | 状态机 | 确保状态管理器唯一 |

**代码示例**：
```java
public enum Logger {
    INSTANCE;
    private PrintWriter writer;
    
    private Logger() {
        // 初始化
    }
    
    public void log(String message) {
        // 记录日志
    }
}
```

#### 5.2.2 工厂模式 + 抽象工厂模式

**应用场景**：对象的创建逻辑复杂或需要动态创建

**实现方式**：
- 简单工厂：根据类型创建对象
- 抽象工厂：统一创建相关对象家族

**应用实例**：

| 工厂类 | 创建对象 | 设计优势 |
|-------|---------|---------|
| ECommerceFactory | Product, PromotionStrategy | 统一管理创建逻辑，解耦客户端与具体类 |

**代码示例**：
```java
public interface AbstractFactory {
    Product createProduct(String type, String id, String name, double price);
    PromotionStrategy createStrategy(String type, Object... params);
}
```

#### 5.2.3 策略模式（Strategy Pattern）

**应用场景**：不同促销类型的折扣计算

**实现方式**：策略接口 + 策略上下文 + 工厂创建

**应用实例**：

| 策略类 | 计算规则 | 扩展性 |
|-------|---------|--------|
| CouponStrategy | 优惠券固定金额 | 新增策略无需修改现有代码 |
| FullReductionStrategy | 满足门槛后减免 | 参数可动态配置 |
| VipDiscountStrategy | 按会员等级折扣 | 等级可扩展 |
| TimeLimitDiscountStrategy | 限时折扣比例 | 时间范围可配置 |
| GiftStrategy | 满足条件赠送 | 赠品可配置 |

**代码示例**：
```java
public interface PromotionStrategy {
    double calculateDiscount(Order order);
}

public class StrategyContext {
    private PromotionStrategy strategy;
    
    public void selectStrategy(String type, Object... params) {
        this.strategy = factory.createStrategy(type, params);
    }
    
    public double executeStrategy(Order order) {
        return strategy.calculateDiscount(order);
    }
}
```

#### 5.2.4 职责链模式（Chain of Responsibility Pattern）

**应用场景**：购物车结算时的促销规则抵扣流程

**实现方式**：处理器链 + 策略注入

**应用实例**：

| 处理器 | 职责 | 链顺序 |
|-------|------|--------|
| CouponHandler | 处理优惠券抵扣 | 第1个 |
| FullReductionHandler | 处理满减抵扣 | 第2个 |
| VipHandler | 处理VIP折扣 | 第3个 |
| TimeLimitDiscountHandler | 处理限时折扣 | 可插入 |
| GiftHandler | 处理满赠 | 可插入 |

**设计优势**：
- 各促销规则节点相互独立
- 新增/删除促销规则只需增删节点
- 调整优先级只需调整节点顺序

**代码示例**：
```java
Handler chain = new DiscountChainBuilder(promotionService)
    .withMember(currentMember)
    .build();
chain.handle(order);
```

#### 5.2.5 观察者模式（Observer Pattern）

**应用场景**：购物车状态变更通知

**实现方式**：传统观察者 + 事件总线

**应用实例**：

| 被观察者 | 观察者 | 事件类型 |
|---------|--------|---------|
| Cart | CartFrame | CART_CHANGED |

**设计优势**：
- 解耦事件发布者和订阅者
- 支持按事件类型管理观察者
- 传统观察者与EventBus并存，增强灵活性

**代码示例**：
```java
// 发布事件
EventBus.getInstance().publishEvent("CART_CHANGED");

// 订阅事件
EventBus.getInstance().registerObserver("CART_CHANGED", this);
```

#### 5.2.6 命令模式（Command Pattern）

**应用场景**：购物车操作的撤销功能

**实现方式**：命令接口 + 命令管理器 + 命令栈

**应用实例**：

| 命令类 | 操作 | 撤销操作 |
|-------|------|---------|
| AddProductCommand | 添加商品 | 删除商品 |
| RemoveProductCommand | 删除商品 | 恢复商品 |
| UpdateQuantityCommand | 修改数量 | 恢复原数量 |
| OrderCommand | 提交订单 | 取消订单 |

**设计优势**：
- 操作可撤销
- 支持批处理和事务
- 命令队列管理

**代码示例**：
```java
CommandManager manager = new CommandManager();
manager.executeCommand(new AddProductCommand(cartService, type, id, name, price, quantity));
manager.undo(); // 撤销
```

#### 5.2.7 模板方法模式（Template Method Pattern）

**应用场景**：订单处理流程

**实现方式**：抽象类定义算法骨架 + 钩子方法

**应用实例**：

| 模板类 | 处理流程 | 钩子方法 |
|-------|---------|---------|
| OrderProcessTemplate | 验证→计算→支付→库存→生成→通知 | needProcessPayment(), needSendNotification() |
| NormalOrderProcessor | 普通订单处理 | 默认实现 |
| VipOrderProcessor | VIP订单处理 | 重写钩子方法 |

**设计优势**：
- 算法骨架固定，细节可变
- 钩子方法提供灵活性
- 代码复用

#### 5.2.8 状态模式（State Pattern）

**应用场景**：订单状态管理

**实现方式**：状态接口 + 状态机管理

**应用实例**：

| 状态类 | 允许操作 | 状态转换 |
|-------|---------|---------|
| PendingPaymentState | 支付、取消 | → PaidState / CancelledState |
| PaidState | 发货 | → ShippedState |
| ShippedState | 确认收货 | → CompletedState |
| CompletedState | 无 | 终态 |
| CancelledState | 无 | 终态 |

**设计优势**：
- 状态转换逻辑集中管理
- 状态机单例，预创建状态实例
- 状态行为封装在状态类中

**代码示例**：
```java
OrderStateMachine.getInstance().initializeState(order);
OrderStateMachine.getInstance().processPayment(order);
OrderStateMachine.getInstance().processShipment(order);
```

#### 5.2.9 适配器模式（Adapter Pattern）

**应用场景**：多种支付方式适配

**实现方式**：接口适配 + 默认适配器

**应用实例**：

| 适配器类 | 支付方式 | 特殊处理 |
|---------|---------|---------|
| AlipayAdapter | 支付宝 | 5%折扣 |
| WechatPayAdapter | 微信支付 | 无折扣 |
| CreditCardAdapter | 信用卡 | 无折扣 |
| BankCardAdapter | 银行卡 | 无折扣 |

**设计优势**：
- 默认适配器减少代码冗余
- 新增支付方式只需继承AbstractPaymentAdapter
- 统一的支付接口，便于扩展

#### 5.2.10 装饰器模式（Decorator Pattern）

**应用场景**：商品价格动态装饰

**实现方式**：组合优于继承

**应用实例**：

| 装饰器类 | 装饰效果 | 应用场景 |
|---------|---------|---------|
| DiscountDecorator | 动态折扣 | 结算时显示折扣后价格 |
| PromotionDecorator | 促销标签 | 商品名称添加促销信息 |

**设计优势**：
- 组合优于继承
- 动态添加职责
- 可以叠加多个装饰器

#### 5.2.11 外观模式（Facade Pattern）

**应用场景**：系统统一入口

**实现方式**：枚举单例 + 服务聚合

**应用实例**：

| 外观类 | 聚合服务 | 设计优势 |
|-------|---------|---------|
| ECommerceFacade | CartService, OrderService, PaymentService等 | 简化接口，降低UI层与Service层耦合 |

**代码示例**：
```java
ECommerceFacade facade = ECommerceFacade.INSTANCE;
CartService cartService = facade.getCartService();
OrderService orderService = facade.getOrderService();
```

### 5.3 设计模式协作关系

#### 5.3.1 模式组合应用

```
外观模式 + 单例模式
ECommerceFacade（枚举单例）
    └── 聚合所有Service
        └── UI层通过INSTANCE访问

职责链模式 + 策略模式 + 工厂模式
CheckoutFrame
    └── 创建StrategyContext
        └── 通过ECommerceFactory创建策略
            └── 注入到Handler
                └── 构建职责链
                    └── 依次处理折扣计算

观察者模式 + 事件总线
Cart（Subject）
    ├── notifyObservers() ──→ CartFrame（Observer）
    └── EventBus.publishEvent("CART_CHANGED")
            └── EventBus通知订阅者

状态模式 + 状态机
OrderService
    └── OrderStateMachine.getInstance()
        ├── initializeState(order)
        ├── processPayment(order)
        ├── processShipment(order)
        └── processCompletion(order)

命令模式 + 命令队列
CartFrame
    └── CommandManager
        ├── executeCommand() ──→ 执行单个命令
        ├── executeBatch() ──→ 批处理
        ├── executeTransaction() ──→ 事务执行
        └── undo() ──→ 撤销
```

## 六、数据模型

### 6.1 核心实体

| 实体 | 核心属性 | 关联实体 |
|-----|---------|---------|
| Product | id, name, price, category | - |
| Cart | items, total | CartItem |
| CartItem | product, quantity, subtotal | Product |
| Order | orderId, items, status, amounts | CartItem, Member, Coupon, Address |
| Member | id, name, level, points | - |
| Coupon | id, name, value, minSpend | - |
| PromotionRule | id, name, type, value, minSpend | - |
| Address | id, name, phone, province, city, district, detailAddress | - |
| Logistics | orderId, status, deliveryMethod, trackingNumber | Order |
| Review | id, productId, userId, rating, content | Product |

### 6.2 数据持久化

| 数据类型 | 持久化方式 | 文件名 |
|---------|-----------|--------|
| 购物车 | 对象序列化 | cart.dat |
| 订单 | 对象序列化 | orders.dat |
| 会员 | 对象序列化 | members.dat |
| 地址 | 对象序列化 | addresses.dat |
| 促销规则 | 对象序列化 | promotions.dat |
| 优惠券 | 对象序列化 | coupons.dat |
| 商品 | 对象序列化 | products.dat |
| 物流 | 对象序列化 | logistics.dat |
| 评价 | 对象序列化 | reviews.dat |
| 日志 | 文件写入 | app.log |

## 七、验收标准

### 7.1 功能验收

| 模块 | 验收项 | 验收标准 |
|-----|--------|---------|
| 购物车管理 | 添加/修改/删除/查询 | 功能正常，金额计算准确 |
| 促销规则管理 | 新增/修改/删除/查询 | 规则参数生效，计算正确 |
| 结算管理 | 优惠计算/订单提交 | 优惠计算准确，订单生成正常 |
| 会员管理 | 等级识别/折扣应用 | 等级折扣正确应用 |
| 订单管理 | 状态流转/历史查询 | 状态转换正确，历史可查 |
| 物流管理 | 物流跟踪/发货确认 | 物流信息正确 |
| 评价管理 | 提交评价/查看评价 | 评价功能正常 |
| 管理员后台 | 各管理模块 | 管理功能正常 |

### 7.2 设计模式验收

| 设计模式 | 验收标准 |
|---------|---------|
| 单例模式 | Logger、ECommerceFacade等类全局唯一实例 |
| 工厂模式 | 正确实现工厂类，可动态创建对象 |
| 策略模式 | 正确定义策略接口及具体策略类，可动态选择策略 |
| 职责链模式 | 正确实现处理链，节点间独立，可灵活调整 |
| 观察者模式 | 正确实现观察者和被观察者，事件通知正常 |
| 命令模式 | 正确实现命令类，支持撤销操作 |
| 模板方法模式 | 正确定义算法骨架，子类可重写步骤 |
| 状态模式 | 正确实现状态类，状态转换正确 |
| 适配器模式 | 正确实现适配器类，接口转换正确 |
| 装饰器模式 | 正确实现装饰器类，动态添加职责 |
| 外观模式 | 正确实现外观类，提供统一入口 |

### 7.3 代码验收

| 验收项 | 验收标准 |
|-------|---------|
| 代码规范 | 命名规范、注释完整 |
| 可运行性 | 代码可正常编译、运行 |
| 可扩展性 | 新增功能无需大量修改原有代码 |
| 设计原则 | 遵循SOLID原则 |

## 八、附录

### 8.1 技术栈

| 技术 | 版本 | 用途 |
|-----|------|------|
| Java | 8+ | 开发语言 |
| Java Swing | - | UI框架 |
| 对象序列化 | - | 数据持久化 |

### 8.2 项目结构

```
com.ecommerce.cart
├── model/              # 实体类
├── service/            # 业务逻辑
├── ui/                 # 界面
├── pattern/            # 设计模式实现
│   ├── singleton/      # 单例模式
│   ├── factory/        # 工厂模式
│   ├── strategy/       # 策略模式
│   ├── chain/          # 职责链模式
│   ├── observer/       # 观察者模式
│   ├── command/        # 命令模式
│   ├── template/       # 模板方法模式
│   ├── state/          # 状态模式
│   ├── adapter/        # 适配器模式
│   ├── decorator/      # 装饰器模式
│   └── facade/         # 外观模式
├── util/               # 工具类
└── Main.java           # 主类
```

### 8.3 运行说明

```bash
# 编译项目
javac -d bin src/main/java/com/ecommerce/cart/**/*.java

# 运行项目
java -cp bin com.ecommerce.cart.Main
```

---

**文档版本历史**

| 版本 | 日期 | 修改内容 |
|-----|------|---------|
| V1.0 | 2026年4月 | 初始版本 |
| V2.0 | 2026年4月20日 | 全面重构，完善功能需求、设计模式说明、验收标准等内容 |
