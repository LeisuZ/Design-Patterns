# 电商购物车结算模块PRD（本科设计模式课程项目）

## 一、文档基础信息

|项目名称|电商购物车结算模块（设计模式课程项目）|
|---|---|
|PRD版本|V1.0|
|作者|（填写自己姓名）|
|撰写日期|（填写撰写日期）|
|适用范围|本科设计模式课程项目，用于演示设计模式在电商促销场景中的应用，适配课程答辩与代码实现|
|修订历史|V1.0：初始版本，完成核心需求与设计模式适配说明，满足课程项目基本要求|
## 二、产品概述

### 2.1 项目背景

本项目为本科设计模式课程实践项目，聚焦电商系统核心的购物车结算模块，针对电商促销活动“规则多变、需求迭代快”的特点，通过应用经典设计模式，实现促销规则的灵活适配、低耦合扩展，解决传统代码中促销逻辑混乱、新增/修改规则成本高的问题，直观体现设计模式在实际业务中的应用价值，完成课程设计任务并满足答辩要求。

### 2.2 核心目标

- 功能目标：实现购物车基础操作、商品结算、多类型促销活动（优惠券、满减、会员折扣）的适配与计算，完成模拟订单生成。

- 设计目标：合理应用职责链模式、策略模式、工厂模式、单例模式4种经典设计模式，实现代码解耦、复用性提升，确保促销规则可灵活新增、修改，体现设计模式应对需求变化的优势。

- 课程目标：通过项目实践，深化对4种设计模式的理解与应用，掌握设计模式解决实际业务问题的思路，完成课程项目考核与答辩演示。

### 2.3 用户画像

- 核心用户：课程授课老师（用于考核设计模式应用合理性、代码规范性）、项目开发者（自身，用于代码实现与答辩讲解）。

- 辅助用户：模拟电商用户（用于演示购物车操作、结算流程、促销规则应用效果）。

### 2.4 核心价值

以电商购物车结算+促销场景为载体，清晰展示4种设计模式的联动应用，解决促销规则多变带来的代码耦合问题，为本科设计模式课程提供可落地、有深度的实践案例，同时培养面向对象设计思维，为后续实际开发奠定基础。

### 2.5 功能范围（包含/不包含）

|包含功能|不包含功能|
|---|---|
|1. 购物车基础操作（添加、修改、删除、查询商品）；2. 多类型促销活动（优惠券、满减、会员折扣）的匹配与计算；3. 促销规则管理（新增、修改、停用）；4. 结算金额实时计算与展示；5. 模拟订单生成与结算记录查询；6. 会员等级识别与折扣适配；7. 日志记录与数据库连接池管理。|1. 复杂用户权限管理（仅实现简单会员识别）；2. 真实支付接口对接（仅模拟支付流程）；3. 高并发处理与分布式部署；4. 复杂前端页面设计（仅实现基础可视化）；5. 商品库存管理、物流对接等非结算相关模块。|
## 三、功能需求（核心部分）

本模块按“购物车管理、促销规则管理、结算管理、会员管理、日志管理”划分核心模块，每个功能明确输入、输出及适配的设计模式，重点体现4种核心设计模式的联动应用。

### 3.1 购物车管理模块

#### 3.1.1 功能1：添加商品到购物车

- 功能描述：用户选择商品，输入购买数量，将商品添加到购物车，自动关联商品基础信息（名称、单价、类型）。

- 输入：商品ID、购买数量。

- 输出：添加成功提示、购物车当前商品列表及总金额（未抵扣促销）。

- 适配设计模式：工厂模式（辅助），通过商品工厂（ProductFactory）根据商品类型（电子产品、服装、食品）动态创建商品对象，避免直接new对象，降低耦合。

#### 3.1.2 功能2：修改购物车商品数量

- 功能描述：用户修改购物车中已有商品的购买数量，修改后实时更新购物车总金额（未抵扣促销）。

- 输入：商品ID、新购买数量。

- 输出：修改成功提示、更新后的购物车列表及总金额。

- 适配设计模式：无（辅助功能，配合核心结算逻辑）。

#### 3.1.3 功能3：删除购物车商品

- 功能描述：用户删除购物车中不需要的商品，删除后实时更新购物车总金额（未抵扣促销）。

- 输入：商品ID。

- 输出：删除成功提示、更新后的购物车列表及总金额。

- 适配设计模式：无（辅助功能）。

#### 3.1.4 功能4：查询购物车商品

- 功能描述：用户查询当前购物车中的所有商品，展示商品名称、单价、数量、小计金额及购物车总金额（未抵扣促销）。

- 输入：无（默认查询当前用户购物车）。

- 输出：购物车商品列表、各商品小计、购物车总金额。

- 适配设计模式：无（辅助功能）。

### 3.2 促销规则管理模块

#### 3.2.1 功能1：新增促销规则

- 功能描述：管理员（模拟）新增促销规则，支持3种类型（优惠券、满减、会员折扣），设置规则参数（如满减金额、折扣比例、优惠券面额）及优先级。

- 输入：促销类型、规则参数、优先级、生效状态。

- 输出：新增成功提示、当前所有促销规则列表。

- 适配设计模式：工厂模式+策略模式，通过促销策略工厂（PromotionStrategyFactory），根据促销类型动态创建对应的促销策略对象（满减策略、折扣策略、优惠券策略），新增促销类型时仅需扩展工厂类和策略类，无需修改核心逻辑。

#### 3.2.2 功能2：修改促销规则

- 功能描述：管理员（模拟）修改已存在的促销规则参数（如调整满减金额、折扣比例）或生效状态（启用/停用）。

- 输入：促销规则ID、修改后的参数/状态。

- 输出：修改成功提示、修改后的促销规则详情。

- 适配设计模式：策略模式，修改规则参数仅需调整对应策略对象的属性，无需改动策略接口及调用逻辑，体现策略模式的灵活性。

#### 3.2.3 功能3：查询促销规则

- 功能描述：管理员（模拟）查询所有促销规则，展示促销类型、规则参数、优先级、生效状态。

- 输入：无（默认查询所有）。

- 输出：促销规则列表，支持按促销类型筛选。

- 适配设计模式：无（辅助功能）。

### 3.3 结算管理模块（核心模块）

#### 3.3.1 功能1：促销规则匹配与金额抵扣

- 功能描述：用户触发结算时，系统自动匹配当前购物车符合条件的促销规则，按预设优先级（如优惠券→满减→会员折扣）依次抵扣，计算最终结算金额。

- 输入：购物车总金额、用户会员等级、已领取优惠券（若有）。

- 输出：各促销规则抵扣金额、抵扣后最终结算金额、促销规则应用明细。

- 适配设计模式：职责链模式+策略模式，将优惠券、满减、会员折扣封装为独立的职责链节点，购物车结算信息依次经过每个节点，每个节点调用对应的促销策略计算抵扣金额，节点间相互独立，可灵活调整优先级或增减节点。

#### 3.3.2 功能2：模拟结算与订单生成

- 功能描述：用户确认结算金额后，模拟支付流程，支付成功后生成订单，记录订单信息（订单号、结算金额、促销抵扣明细、下单时间）。

- 输入：用户确认结算的指令、支付方式（模拟）。

- 输出：支付成功提示、订单详情、订单号。

- 适配设计模式：单例模式（辅助），通过日志记录器（Logger）单例，记录订单生成、结算过程的日志，便于调试与答辩演示。

#### 3.3.3 功能3：结算记录查询

- 功能描述：用户查询历史结算记录，展示订单号、结算金额、促销抵扣明细、下单时间、支付状态。

- 输入：无（默认查询当前用户所有结算记录）。

- 输出：结算记录列表，支持按时间筛选。

- 适配设计模式：单例模式（辅助），通过数据库连接池（DBConnectionPool）单例，统一管理数据库连接，提升查询性能，避免连接冗余。

### 3.4 会员管理模块（简化）

#### 3.4.1 功能：会员等级识别

- 功能描述：模拟会员等级识别，支持普通会员、VIP会员两个等级，不同等级对应不同折扣比例（普通会员无折扣，VIP会员9折）。

- 输入：用户ID。

- 输出：用户会员等级、对应折扣比例。

- 适配设计模式：策略模式，将不同会员等级的折扣逻辑封装为独立的折扣策略，根据会员等级动态选择对应的策略计算折扣金额。

### 3.5 日志管理模块（简化）

#### 3.5.1 功能：结算日志记录

- 功能描述：记录购物车操作、结算过程、促销规则应用、订单生成等关键操作的日志，便于调试和答辩演示。

- 输入：操作类型、操作内容、操作时间。

- 输出：日志记录（存储于数据库，可查询）。

- 适配设计模式：单例模式，日志记录器（Logger）全局唯一实例，确保所有操作日志统一记录，避免日志冗余。

## 四、非功能性需求

- 运行稳定性：系统可正常启动，核心功能（购物车操作、结算、促销计算）无报错，连续运行无崩溃。

- 代码可复用性：基于设计模式实现，类结构清晰，促销规则、策略等模块可灵活扩展，新增功能时无需大量修改原有代码。

- 操作便捷性：界面简洁（控制台或简单Swing/JSP），操作流程清晰，便于答辩演示。

- 环境适配性：适配常用开发环境，如JDK1.8、MySQL8.0，无需复杂配置即可运行。

- 日志完整性：关键操作（结算、促销应用、订单生成）均有日志记录，可追溯操作过程。

## 五、设计模式应用说明（重点）

本项目核心应用4种经典设计模式，重点解决电商促销场景中“规则多变、耦合度高”的问题，各模式联动应用，体现设计模式的实际价值，具体应用说明如下：

### 5.1 职责链模式（核心应用）

- 应用场景：购物车结算时的促销规则抵扣流程。

- 核心作用：将优惠券抵扣、满减计算、会员折扣3种促销规则，分别封装为独立的职责链节点（CouponHandler、FullReductionHandler、VIPDiscountHandler），串联成一条处理链。结算时，购物车信息依次经过每个节点，每个节点仅处理自身负责的促销规则（如CouponHandler仅校验优惠券有效性并计算抵扣金额），处理完成后将信息传递给下一个节点，最终得到最终结算金额。

- 优势：各促销规则节点相互独立，新增/删除促销规则（如新增“限时折扣”）时，仅需新增对应的节点并加入链条，无需修改核心结算逻辑；调整促销优先级时，仅需调整节点顺序，极大降低了代码耦合度，应对需求变化更灵活。

### 5.2 策略模式（核心应用）

- 应用场景：不同促销规则的折扣/抵扣算法实现。

- 核心作用：定义统一的促销策略接口（PromotionStrategy），封装不同的促销算法，实现3种具体策略：满减策略（FullReductionStrategy）、折扣策略（DiscountStrategy）、优惠券策略（CouponStrategy）。每种策略独立实现“计算优惠金额”的方法，结算时根据促销类型动态选择对应的策略。

- 优势：算法与调用逻辑解耦，修改某类促销的计算规则（如将VIP折扣从9折改为8.5折）时，仅需修改对应策略类，无需改动调用代码；新增促销算法（如“买一送一”）时，仅需新增策略类并实现接口，符合“开放-封闭原则”。

### 5.3 工厂模式（配套应用）

- 应用场景：促销策略对象、商品对象的动态创建。

- 核心作用：创建两个工厂类，分别是促销策略工厂（PromotionStrategyFactory）和商品工厂（ProductFactory）。PromotionStrategyFactory根据促销类型（如“满减”“折扣”）动态创建对应的策略对象；ProductFactory根据商品类型（如“电子产品”“服装”）动态创建对应的商品对象。

- 优势：避免在核心逻辑中直接new对象，降低代码耦合度；新增促销类型或商品类型时，仅需扩展工厂类和对应实体类，无需修改原有调用逻辑，提升代码可扩展性。

### 5.4 单例模式（基础应用）

- 应用场景：数据库连接池、日志记录器、促销规则配置管理器。

- 核心作用：确保这3个核心工具类/管理类全局只有一个实例。数据库连接池（DBConnectionPool）避免频繁创建/关闭数据库连接，提升系统性能；日志记录器（Logger）确保所有操作日志统一记录，避免日志冗余；促销规则配置管理器（PromotionConfigManager）统一管理所有促销规则配置，避免配置混乱。

- 优势：节省系统资源，避免实例重复创建导致的性能损耗；确保全局数据统一，便于管理和维护。

## 六、验收标准（贴合本科课程要求）

### 6.1 功能验收

- 购物车管理：添加、修改、删除、查询商品功能正常，金额计算准确。

- 促销规则管理：新增、修改、查询促销规则功能正常，规则参数生效。

- 结算管理：促销规则匹配准确，抵扣金额计算正确，订单生成正常，结算记录可查询。

- 会员管理：会员等级识别正常，对应折扣比例应用准确。

- 日志管理：关键操作日志记录完整，可正常查询。

### 6.2 设计模式验收

- 职责链模式：正确实现促销规则处理链，节点间独立，可灵活调整优先级、增减节点。

- 策略模式：正确定义策略接口及具体策略类，可动态选择促销算法，支持策略扩展。

- 工厂模式：正确实现策略工厂和商品工厂，可动态创建对应对象，支持类型扩展。

- 单例模式：数据库连接池、日志记录器等类实现单例，确保全局唯一实例。

- 类结构清晰，设计模式应用合理，能体现解耦、复用、灵活扩展的优势。

### 6.3 代码验收

- 代码规范：命名规范、注释完整，重点标注设计模式的应用场景和核心逻辑。

- 可运行性：代码可正常编译、运行，无语法错误、逻辑错误。

- 可扩展性：新增促销规则、商品类型时，无需大量修改原有代码，符合设计模式的设计思想。

## 七、补充说明

本PRD贴合本科设计模式课程项目要求，重点突出设计模式的应用，简化了复杂的电商业务逻辑，聚焦购物车结算与促销模块，便于代码实现和答辩演示。后续可根据实际开发需求，补充细节（如具体的数据库表结构、前端界面原型），也可扩展更多促销类型（如限时折扣、组合优惠），进一步体现设计模式的灵活性。
> （注：文档部分内容可能由 AI 生成）